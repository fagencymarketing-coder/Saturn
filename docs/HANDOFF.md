# Сатурн — точка входа в проект

**Читать первым делом в любом новом чате.** Обновлено: 2026-09-23.

## Главное правило
🔴 **Вся работа ведётся ТОЛЬКО в репозитории `fagencymarketing-coder/saturn`,
ветка `main`.** Репозиторий `fagencymarketing-coder/-` — устаревший, в нём нет
ничего, чего нет здесь. Туда не коммитить.

## Кто и что
- Заказчик: ООО «Сатурн» — B2B, оптовые поставки удобрений, СЗР и семян +
  сервис (диагностика, подбор, поставка, агросопровождение).
- Домен: **sssaturn.ru** (Tilda, SSL выпущен, A-запись → 176.57.66.96).
- Роли: дизайнер-заказчик работает в **Claude Design** (макет), я (Claude Code) —
  senior full-cycle: контекст, данные, ассеты, промты для Claude Design, git.
  Финальная сборка — **Tilda**.

## Где мы сейчас (сентябрь 2026)
Главная страница **свёрстана в Claude Design** и выверена по единой дизайн-системе.
Прототип `prototype/site-prototype-v44.html` — это СТАРАЯ блок-схема, не визуал.

Структура главной (8 секций, чередование свет/тёмное):
Герой (бел) → Каталог (`#F7F7F6`) → **Полный цикл** (тёмный, фото поля) →
Результаты испытаний (бел) → **География** (тёмная, карта) → Документы (`#F7F7F6`)
→ **Контакты** (тёмный) → Футер (светлый).

## Карта репозитория
| Папка | Что внутри |
|---|---|
| `docs/` | **Вся документация.** `DESIGN-VISION.md` — дизайн-камертон и журнал решений (главный файл). `COMPANY.md` — подтверждённые данные и запреты. `CLIENT-REQUESTS.md` — что ждём от заказчика. `DEALER-MODEL.md` — дилерская модель. |
| `brand/` | Логобук в токенах (`tokens.json`, `variables.css`) — источник правды по бренду |
| `design/` | `geo-map/` — генератор карты (10 регионов, реальные границы). `results-block/` — эталон блока кейсов |
| `catalogs/` | Прайс-2026 (xlsx), `catalog_full.csv/json` — 63 позиции, `HOMEPAGE-CATALOG-6.md` — 6 позиций для главной |
| `assets/` | `product-photos/` (45 фото + реестр), `documents/` (сертификаты + превью + страницы для лайтбокса), `icons-new/` (Phosphor) |
| `source-files/` | Оригиналы документов заказчика |
| `archive/` | Исторические материалы (старые прототипы, мокапы, заглушка) — не рабочая область |

## Ключевые решения (детали — в `docs/DESIGN-VISION.md`)
- Эстетика: **«тихая дороговизна»** — воздух, сдержанность, один оранжевый акцент.
- Токены: `#FF4200` · `#1A1A1A` · `#FFFFFF` · `#F7F7F6` · тёмный `#141210`.
  Montserrat. **Зелёного в бренде нет.**
- Карта: 10 регионов поставок (добавлены Кемеровская и Томская).
- Документы: блок «Документы и сертификаты» — сертификат дилера «Фертика» +
  3 СГР «Волски Биохим». На Tilda открываются **в лайтбоксе, без скачивания**.

## ⚠️ Не публиковать
- 🔴 **Документы «Генезис»** — решение заказчика: на сайт не выкладываем.
- «12 лет на рынке» — не подтверждено (ОГРН 2023). Заменить на цифры испытаний-2025.
- Genesis как бренд публично не показываем (продукция идёт под Relict Organics).
- 🔴 БЕЗОПАСНОСТЬ: в переписке был пароль reg.ru — сменить, включить 2FA.
  Файлы `chat.md`/`chat.txt` в `.gitignore` на всех уровнях.

## Ближайшие задачи
1. Заменить фото героя (рассвет/зелёное поле вместо заката).
2. ✅ Цифры испытаний-2025 — сделано и принято в макете (строка статистики
   + перебранная геометрия карточек, эталон в `DESIGN-VISION.md`).
3. Отдельные страницы: карточка товара, полный каталог (данные готовы).
4. Вопросы заказчику: бренд **OYYO** (12 позиций без цен), цены на 15 позиций.
5. Перенос на Tilda, MX-записи, редирект relict-altay.ru → sssaturn.ru.
6. Перед Tilda: свести два почти одинаковых светлых тона `#F6F5F3` и `#F7F7F6`
   к одному — иначе в глобальных переменных Tilda их перепутают.
7. Запросить у заказчика тех. листы (состав, д.в., норма расхода, культуры) —
   без них страница товара остаётся витриной, а не справочником.
